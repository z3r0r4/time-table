<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">
    <!--checkable makes sure that only one can be selected
        all icons should be contained in here
        change ic_launcher to the fitting icons from drawable-->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_ovp"
            android:icon="@drawable/ic_launcher_background"
            android:title="@string/ovp_fragment_title"/>
        <item
            android:id="@+id/nav_timetable"
            android:icon="@drawable/ic_launcher_background"
            android:title="@string/my_tt_fragment_title"/>
        <item
            android:id="@+id/nav_notifications"
            android:icon="@drawable/ic_launcher_background"
            android:title="@string/notifications_fragment_title"/>
    </group>

    <item android:title="@string/drawer_divider_title">
        <menu>
            <item
                android:id="@+id/nav_login"
                android:icon="@drawable/ic_launcher_background"
                android:title="@string/login_button_title"/>
        </menu>
    </item>
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_launcher_background"
            android:title="@string/settings_fragment_title"/>
    </group>

</menu>